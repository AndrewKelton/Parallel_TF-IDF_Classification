#include <iostream>
#include "file_operations.h"
#include "utils.h"

int main(int argc, char * argv[]) {

    if (argc < 2) {
        cerr << "not enough inputs..." << endl;
        exit(EXIT_FAILURE);
    }

    
}
